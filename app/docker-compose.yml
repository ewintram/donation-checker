version: "3.9"
services:
  api:
    build: .
    image: donation-checker-dev
    container_name: donation-checker-api
    volumes:
      - .:/app
      - ./dist:/app/dist
    ports:
      - 3000:80
    entrypoint: "npm run dev"
